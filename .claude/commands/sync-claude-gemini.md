---
description: .claude/ と .gemini/ のスキル・コマンド・ワークフローを同期
---

# Claude-Gemini 設定同期

`.claude/` と `.gemini/` のコマンド・スキル・ワークフローを双方向に同期します。

**必須**: Skillツールを使用して `sync-claude-gemini` スキルを実行すること。

```
Skill(skill: "sync-claude-gemini")
```

> **スキル参照**: `.claude/skills/sync-claude-gemini/SKILL.md`
