---
description: 新規記事フォルダを作成し、テンプレートから初期構造を生成します。記事メタデータの設定とワークフロー管理の準備を行います。
argument-hint: [トピック名]
---

新規記事フォルダを作成し、執筆ワークフローの準備をします。

## 入力パラメータの解析

ユーザーが指定したパラメータを確認します：

-   **トピック名** (必須): 記事のテーマ（例: `D.B.クーパー事件`, `如月駅`, `ヴォイニッチ写本`）

トピック名が指定されていない場合は、ユーザーに確認してください。

## 処理フロー

### Phase 1: 記事情報の収集

1. **トピック名の確認**

    引数からトピック名を取得。指定がない場合はユーザーに質問します。

    ```
    記事のトピックを教えてください（例: D.B.クーパー事件）:
    ```

2. **カテゴリの選択**

    以下のカテゴリから選択してもらいます：

    -   `unsolved`: 未解決事件
    -   `urban`: 都市伝説・怪談
    -   `unidentified`: 不思議現象・UFO
    -   `history`: 歴史の謎・オカルト

    ユーザーに質問：

    ```
    カテゴリを選択してください:
    1. unsolved (未解決事件)
    2. urban (都市伝説・怪談)
    3. unidentified (不思議現象・UFO)
    4. history (歴史の謎・オカルト)
    ```

3. **英語テーマ名の生成**

    トピック名から英語のテーマ名を生成します：

    -   ケバブケース（小文字、ハイフン区切り）
    -   特殊文字を除去
    -   例: `D.B.クーパー事件` → `db-cooper`
    -   例: `如月駅` → `kisaragi-station`
    -   例: `ヴォイニッチ写本` → `voynich-manuscript`

    生成した英語名をユーザーに確認：

    ```
    英語テーマ名: {theme_name_en}
    この名前でよろしいですか？ (y/n または修正案を入力)
    ```

### Phase 2: article_id の生成と重複確認

4. **既存記事の確認**

    `articles/` ディレクトリ内の既存記事を確認し、同じカテゴリの最大通番を取得します。

    ```bash
    ls -d articles/{category}_* 2>/dev/null | grep -o '{category}_[0-9]*' | cut -d'_' -f2 | sort -n | tail -1
    ```

    通番は既存の最大値 + 1、または存在しない場合は 001 から開始します。

5. **article_id の生成**

    形式: `{category}_{seq:03d}_{theme_name_en}`

    例:

    -   `unsolved_002_db-cooper`
    -   `urban_003_kisaragi-station`
    -   `history_001_voynich-manuscript`

6. **重複チェック**

    `articles/{article_id}/` が既に存在しないことを確認します。
    存在する場合はエラーを表示し、処理を中断します。

### Phase 3: フォルダ構造の作成

7. **TodoWrite でタスク作成**

    ```
    - 記事フォルダ作成
    - テンプレートファイルのコピー
    - メタデータの初期化
    - 完了報告
    ```

8. **ディレクトリ作成**

    ```bash
    mkdir -p articles/{article_id}
    ```

9. **テンプレートのコピー**

    `templates/{article_id}-theme-name-en/` から `articles/{article_id}/` へ全ファイルをコピーします。

    ```bash
    cp -r templates/\{article_id\}-theme-name-en/* articles/{article_id}/
    ```

    コピーされるファイル構造:

    ```
    articles/{article_id}/
    ├── article-meta.json
    ├── 01_research/
    │   ├── queries.json
    │   ├── raw-data.json
    │   ├── sources.json
    │   ├── claims.json
    │   ├── analysis.json
    │   ├── decisions.json
    │   ├── fact-checks.json
    │   ├── checks.json
    │   └── visualize/
    │       ├── summary.md
    │       ├── timeline.md
    │       ├── relations.md
    │       └── dashboard.md
    ├── 02_edit/
    │   ├── first_draft.md
    │   ├── critic.json
    │   ├── critic.md
    │   └── revised_draft.md
    └── 03_published/
        └── YYYYMMDD_article-title-en.md
    ```

### Phase 4: メタデータの初期化

10. **article-meta.json の更新**

    `articles/{article_id}/article-meta.json` を以下の内容で更新します：

    ```json
    {
        "article_id": "{article_id}",
        "topic": "{トピック名}",
        "category": "{category}",
        "tags": [],
        "status": "research",
        "created_at": "{YYYY-MM-DD}",
        "updated_at": "{YYYY-MM-DD}",
        "workflow": {
            "research": {
                "queries": "pending",
                "raw_data": "pending",
                "sources": "pending",
                "claims": "pending",
                "analysis": "pending",
                "iterations_completed": 0,
                "depth_completed": 0,
                "fact_checks": "pending",
                "decisions": "pending",
                "visualize": "pending"
            },
            "writing": {
                "first_draft": "pending",
                "checks": "pending",
                "revised_draft": "pending"
            },
            "publishing": {
                "final_review": "pending",
                "published": "pending"
            }
        }
    }
    ```

11. **空ファイルの article_id 更新**

    以下のファイルの `article_id` フィールドを更新：

    -   `01_research/queries.json`
    -   `01_research/raw-data.json`
    -   `01_research/sources.json`
    -   `01_research/claims.json`
    -   `01_research/analysis.json`
    -   `01_research/decisions.json`
    -   `01_research/fact-checks.json`
    -   `01_research/checks.json`

    各ファイルの `"article_id": ""` を `"article_id": "{article_id}"` に置換します。

### Phase 5: 完了報告

12. **成功メッセージの表示**

    ```markdown
    ✅ **新規記事フォルダを作成しました**

    ## 📋 記事情報

    -   **記事 ID**: {article_id}
    -   **トピック**: {トピック名}
    -   **カテゴリ**: {category_label}
    -   **作成日**: {YYYY-MM-DD}
    -   **フォルダ**: `articles/{article_id}/`

    ## 📂 作成されたファイル

    -   メタデータ: `article-meta.json`
    -   リサーチ: `01_research/` (8 ファイル + visualize/)
    -   執筆: `02_edit/` (4 ファイル)
    -   公開: `03_published/` (1 テンプレート)

    ## 🚀 次のステップ

    1. **リサーチ開始**: `/research --article {article_id}`
    2. **執筆開始**: `/edit {article_id}` (リサーチ完了後)

    ## 💡 ヒント

    -   記事の進捗は `article-meta.json` で管理されます
    -   `/research` で2段階リサーチ（shallow → deep）が自動実行されます
    -   `/edit` で初稿作成→批評→修正の全工程が自動実行されます
    -   `--parallel` オプションで並列処理モードが利用可能です
    ```

## エラーハンドリング

### articles/ ディレクトリが存在しない場合

```bash
mkdir -p articles/
```

自動的に作成してから処理を続行します。

### 重複する article_id が存在する場合

```
❌ エラー: 記事フォルダが既に存在します

既存フォルダ: articles/{article_id}/

💡 **対処法**:
- 別の英語テーマ名を使用してください
- または既存フォルダを削除してから再実行してください
```

### テンプレートフォルダが存在しない場合

```
❌ エラー: テンプレートフォルダが見つかりません

必要なパス: templates/{article_id}-theme-name-en/

💡 **対処法**:
- プロジェクトのセットアップが完了していない可能性があります
- `/initialize-project` を実行してプロジェクトを初期化してください
```

### ファイルのコピーに失敗した場合

```
⚠️ 警告: 一部のファイルのコピーに失敗しました

成功: {count} ファイル
失敗: {count} ファイル

処理を継続します。不足しているファイルは後で手動で作成してください。
```

## ベストプラクティス

1. **明確な命名**

    -   英語テーマ名は短く、わかりやすく
    -   ケバブケースを厳守（小文字 + ハイフン）
    -   固有名詞は音訳または意訳で表現

2. **カテゴリの一貫性**

    -   迷った場合はトピックの主要な性質で判断
    -   複数カテゴリにまたがる場合は、より中心的なものを選択

3. **メタデータの充実**
    -   作成後、`article-meta.json` の `tags` フィールドを手動で追加推奨
    -   タグ例: `["未解決事件", "ハイジャック", "失踪"]`

## プロジェクト固有の情報

このコマンドはミステリーブログ執筆プロジェクトで使用されます：

-   **プロジェクトルート**: `/Users/yuki/Desktop/prj-note`
-   **記事フォルダ**: `articles/{article_id}/`
-   **テンプレート**: `templates/{article_id}-theme-name-en/`
-   **カテゴリ定義**: `data/categories.json`
-   **関連コマンド**: `/research`, `/edit`, `/wiki-search`, `/suggest-topics`
-   **関連エージェント**: research-manager (リサーチ統括), edit-manager (執筆統括)
